# Kleinanzeigen Order Tool v1.4

Fix: Kontaktparser läuft jetzt serverseitig über `/api/parse-contact`, zeigt erkannte Felder mit Belegen an und füllt die Kundenfelder robuster aus.

Update: Inhalt ins GitHub-Repo hochladen, dann auf dem Server `git fetch --all && git reset --hard origin/main && ./venv/bin/pip install -r requirements.txt && systemctl restart kleinanzeigen-order-tool`.
